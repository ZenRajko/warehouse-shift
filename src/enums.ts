export enum Page {
  Title,
  Intro,
  Info,
  Game,
  Win,
  Lose
}

export enum Crate {
  Wood = '#',
  Metal = '@'
}

export enum Icon {
  Wood = "border_all",
  Metal = "margin",
  Target = "indeterminate_question_box"
}

export enum BackgroundType {
  Paper = "paper",
  Brick = "brick"
}
